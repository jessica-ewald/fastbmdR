This R package is for benchmark dose analysis of omics data. To install:

1. Install and load the devtools R package:
```
install.packages("devtools")
library(devtools)
```

2. Install and load fastbmdR:
```
install_github("jessica-ewald/fastbmdR")
library(fastbmdR)
```
